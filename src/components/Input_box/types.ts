export type Inputprops = {
  type?: "text" | "password";
  variant?: "default" | "error" | "password_view" | "password_hidden" | "info";
  disabled?: boolean;
  placeholder?: string;
  lname: string;
  infoText?: string;
};
